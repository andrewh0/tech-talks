enum VideoSource {
  YOUTUBE,
  VIMEO
}

enum EventType {
  MEETUP,
  CONFERENCE
}

type Talk {
  id: ID! @unique
  event: Event
  speakers: [Speaker!]!
  description: String
  duration: Int
  hidden: Boolean
  private: Boolean
  publishedAt: DateTime
  source: VideoSource
  title: String!
  thumbnailUrl: String
  videoUrl: String
  viewCount: Int
}

type Speaker {
  id: ID! @unique
  events: [Event!]!
  talks: [Talk!]!
  name: String!
  twitterHandle: String
}

type Event {
  id: ID! @unique
  speakers: [Speaker!]!
  talks: [Talk!]!
  city: String
  country: String
  endDate: String
  name: String
  startDate: String
  type: EventType
  youtubePlaylist: String
}

type Organization {
  id: ID! @unique
  events: [Event!]!
  twitterHandle: String
  website: String
  youtubeChannel: String
}
